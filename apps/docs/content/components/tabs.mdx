import { TabsDemo as Tabs } from "@/components/demos/tabs/tabs-demo";
import { TabsDemo } from "@/components/tabs-demo";

# Tabs

Component used to split content into different views.

<TabsDemo demoContent={<Tabs />}>
```tsx
export function TabsDemo() {
  return (
    <Tabs className="w-1/2 mx-auto">
      <Tabs.List>
        <Tabs.Trigger value="account">
          {" "}
          <User
            className="inline-block mr-2"
            size={16}
          />
          Account
        </Tabs.Trigger>
        <Tabs.Trigger value="settings">
          {" "}
          <Settings
            className="inline-block mr-2"
            size={16}
          />
          Settings
        </Tabs.Trigger>
      </Tabs.List>
      <Tabs.Content value="account">Account content</Tabs.Content>
      <Tabs.Content value="settings">Settings Content</Tabs.Content>
    </Tabs>
  );
}

```
</TabsDemo>
```
