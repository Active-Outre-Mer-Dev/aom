import { Meta, Canvas, Primary, Controls } from "@storybook/blocks";
import * as DialogStories from "./Dialog.story";
import { Dialog } from "@aomdev/ui";

<Meta of={DialogStories} />

<div className="flex gap-2 items-center">
  <h1>Dialog</h1>
  <Badge color={"success"}>Complete</Badge>
</div>

A dialog is used to display a window overlaid on a primary window.

<Canvas of={DialogStories.DefaultDialog} />

## Props

<Controls /> 